# World-Events Pipeline — Python dependencies
#
# Core (always required)
requests>=2.31.0
mcp>=1.0.0

# Embeddings (MiniLM semantic ranking)
sentence-transformers>=2.7.0
numpy>=1.24.0

# LLM (Ollama Cloud)
ollama>=0.2.0

# Visualisation
matplotlib>=3.8.0

# Optional — rich coloured logging (Jupyter/Colab)
rich>=13.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
